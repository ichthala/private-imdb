<h1>Movies Found:</h1>

<ul>
<% @movies.each do |movie| %>
  <li>

    <%= link_to( movie.title, "/search_results/#{movie.id}" ) %>

    <%= button_to "Save", movie_create_path(movie.id) %>

    <%= button_to "Save & Favorite", "/movies/#{movie.id}/favorite_result" %>

  </li>
<% end %>
</ul>